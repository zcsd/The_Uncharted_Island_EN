function User(username, nickname, sex, coins, whichavatar){
    this.username = username,
    this.nickname = nickname,
    this.sex = sex,
    this.coins = coins,
    this.whichavatar = whichavatar
}

module.exports = User;